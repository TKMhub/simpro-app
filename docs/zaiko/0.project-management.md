# Zaiko プロジェクト管理

Zaikoアプリの開発タスク、進捗、ロードマップを管理します。

## 1. ロードマップ
開発プロセス（`docs/project-rules.md`）に基づき、以下の順序で進行します。

- **Phase 1: UI/UX 実装** (画面のガワ作成)
- **Phase 2: バックエンド構築** (DBスキーマ・モデル)
- **Phase 3: API & ロジック実装** (データ連携)
- **Phase 4: 機能拡張 & テスト** (通知・共有・仕上げ)

## 2. タスク一覧

### Phase 1: UI/UX 実装（画面・コンポーネント）✅ **完了**
画面の見た目とインタラクションを先行して実装します。データはモックを使用します。

#### 1.1 基盤・共通コンポーネント
- [x] **Zaiko専用レイアウト構築** (`layout/zaiko-shell.tsx`, `layout.tsx`)
    - [x] スマホ幅（max-w-md等）のコンテナ作成
    - [x] ヘッダー (`zaiko-header`) 実装
    - [x] 背景・テーマ設定
- [x] **LP実装** (`page.tsx`)
    - [x] Heroセクション (`ui-hero`)
    - [x] 機能紹介・メリットセクション
    - [x] アニメーション適用 (Framer Motion)
- [x] **共通UIコンポーネント作成** (`_components/common`)
    - [x] `zaiko-bottom-sheet` (スライドアップメニュー)
    - [x] `fab-add-button` (追加ボタン)
    - [x] `alert-banner` (アラート帯)

#### 1.2 認証画面
- [x] **ログイン画面実装** (`login/page.tsx`)
    - [x] デザイン調整（Zaikoテーマ適用）
    - [x] フォームUI（機能は後回し）

#### 1.3 ダッシュボード（在庫一覧）
- [x] **在庫カード実装** (`inventory/inventory-card.tsx`)
    - [x] アイコン、名称、残量、ステータス表示
    - [x] ステータスバッジのスタイル定義
- [x] **在庫リスト実装** (`inventory/inventory-list.tsx`)
    - [x] モックデータでの表示確認
    - [x] カテゴリフィルタチップ (`inventory-filter-chips`) 実装
- [x] **ダッシュボードページ組立** (`dashboard/page.tsx`)
    - [x] ヘッダー、アラート帯、リスト、FABの配置

#### 1.4 詳細・登録・編集画面
- [x] **入力フォームコンポーネント** (`inventory/inventory-detail-form.tsx`)
    - [x] アイテム名、アイコン選択、カテゴリ、場所
    - [x] 数量ステッパー (`inventory-quantity-stepper`)
    - [x] Zodスキーマ定義 (`inventory-schema.ts`)
- [x] **各ページ実装**
    - [x] 新規登録（単身） (`input/page.tsx`)
    - [x] 新規登録（家族） (`[familyName]/input/page.tsx`)
    - [x] 詳細・編集 (`detail/[itemId]/page.tsx`)

#### 1.5 その他画面
- [x] **メンバー管理画面** (`member/page.tsx`)
- [x] **買い物リスト画面** (`tobuy/page.tsx`)
- [x] **通知履歴画面** (`alert/page.tsx`)
- [x] **設定画面** (`settings/page.tsx`)

---

### Phase 2: バックエンド構築（DB）
UIで必要なデータ構造に基づき、Supabaseのスキーマを定義します。

#### 2.1 データベース設計
- [ ] **ER図・スキーマ定義作成** (`docs/zaiko/4.database-design.md`)
    - [ ] Users (Profile)
    - [ ] Families / Groups
    - [ ] Items (Inventory)
    - [ ] Categories (Master)
    - [ ] Locations (Master or Custom)

#### 2.2 マイグレーション実行
- [ ] **Prismaスキーマ更新** (`schema.prisma`)
- [ ] **マイグレーション適用** (`migrate dev`)

---

### Phase 3: API & ロジック実装
UIとDBを接続します。

#### 3.1 API / Server Actions
- [ ] **在庫CRUD実装** (`api/inventory/route.ts` or Server Actions)
    - [ ] Create, Read, Update, Delete
    - [ ] ステータス自動判定ロジック
- [ ] **メンバー管理API**
    - [ ] 招待、権限変更
- [ ] **買い物リスト生成ロジック**

#### 3.2 UI連携
- [ ] **Hooks実装** (`_hooks/`)
    - [ ] `use-inventory-list` (SWR or React Query)
    - [ ] `use-inventory-detail`
- [ ] **コンポーネントへの組み込み**
    - [ ] ダッシュボードのデータ連携
    - [ ] フォームの送信処理実装

---

### Phase 4: 機能拡張 & 仕上げ

#### 4.1 通知機能
- [ ] **閾値アラート通知**
- [ ] **在庫切れ通知**

#### 4.2 仕上げ・テスト
- [ ] **E2Eテスト / 動作確認**
- [ ] **デザイン微調整**
- [ ] **パフォーマンス最適化**

## 3. 現在のステータス
- **Current Phase**: Phase 1 完了 ✅ → Phase 2 へ移行準備中
- **Next Action**: Phase 2 - データベース設計・スキーマ定義

## 4. 完了したタスク

### Phase 1: UI/UX 実装 ✅ 完了（2025年11月29日）

**実装内容:**
- ✅ 全11画面のUI実装完了
- ✅ 20以上のコンポーネント作成
- ✅ Framer Motion によるアニメーション実装
- ✅ モックデータでの動作確認可能
- ✅ スマホネイティブアプリ風デザイン
- ✅ ライト/ダークモード対応
- ✅ レスポンシブ対応
- ✅ Linterエラー 0件

**実装した画面:**
1. ランディングページ (LP)
2. ログイン画面
3. ダッシュボード（在庫一覧）
4. 在庫詳細・編集画面
5. 在庫新規登録（単身用）
6. 在庫新規登録（家族用）
7. メンバー管理画面
8. 買い物リスト画面
9. 通知履歴画面
10. 設定画面

**詳細レポート:** `docs/zaiko/phase1-ui-completion.md` を参照